version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: easy-erp-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: easy_erp_db
      MYSQL_USER: erp_user
      MYSQL_PASSWORD: erp_password
    ports:
      - '3306:3306'
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql/init:/docker-entrypoint-initdb.d
    command: --default-authentication-plugin=mysql_native_password

  redis:
    image: redis:7-alpine
    container_name: easy-erp-redis
    restart: always
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

  # 如果使用Docker部署应用，添加以下配置
  # easy-erp-web:
  #   build: .
  #   container_name: easy-erp-web
  #   restart: always
  #   ports:
  #     - '3008:3008'
  #   volumes:
  #     - uploads_data:/app/public/uploads
  #   environment:
  #     - NODE_ENV=production
  #   depends_on:
  #     - mysql
  #     - redis

volumes:
  mysql_data:
  redis_data:
  uploads_data:  # 用于持久化存储上传文件
